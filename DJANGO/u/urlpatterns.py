

# 'admin/' é a rota editável, podendo ser qualquer outra string
def pp_urls():
    """
    from django.contrib import admin
    from django.urls import path

    urlpatterns = [path('admin/', admin.site.urls)]
    """
